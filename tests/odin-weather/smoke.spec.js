// App loads and main UI is visible (search input + search button or Enter hint)
// Search a known valid city (e.g. “London”) and results render (at least city/title + one weather value)
// Loading state appears then disappears (spinner/loading text goes away) OR results container becomes visible
// Search can be triggered via button click
// Search can be triggered via Enter key
